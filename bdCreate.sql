CREATE TABLE generalInfo(
usedID             varchar(80),
appVersion varchar(40),
compilerVersion real,
typeCompiler varchar(10),
language varchar(20),
glslVersion  varchar(20),
renderer varchar(50),
vendor varchar(30),
os    varchar(10),
version varchar(15),
qtVersion varchar(10),
screenDpi int,
screenHeight int,
screenWidth int
);
